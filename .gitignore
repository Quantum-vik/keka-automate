# Secrets and state — never commit these
.env
session.json
logs/
.venv/
__pycache__/
*.pyc

# design handoff artifacts
Better UI designs.zip
*.zip
Auto-Keka.app/
*.icns
packaging/auto-keka.ico

# Note: Auto-Keka.command / .sh / .bat are the tracked double-click launchers.

# Licensing secrets — NEVER commit
tools/license_private.pem
license.key
